{
  "dependencies": {
    "cors": "^2.8.5",
    "minio": "^8.0.5",
    "multer": "^2.0.2",
    "oracledb": "^6.9.0",
    "solc": "^0.8.30",
    "web3": "^4.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update-ip": "node get-ip.js",
    "start-with-ip": "npm run update-ip && cd chat-api && node index.js",
    "dev": "npm run update-ip && cd frontend && npm run dev"
  }
}